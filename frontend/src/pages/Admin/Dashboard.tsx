import React from "react";

interface DashboardProps {
  setDash: (dash: string) => void;
}

const Dashboard: React.FC<DashboardProps> = ({ setDash }) => {
  return (
    <div ></div>
  );
};

export default Dashboard;
